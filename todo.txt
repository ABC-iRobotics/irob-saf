TODO:
	- Preempt check for all action servers needed?
	- Find cause of random crashes (may be moving too fast?)
	- Set execution speed in launch file
	- Change high level units to mm
	- Implement camera coord frame transform
	- High level nodes should know if the low level ones are failed 
		(while waiting for isDone())
		
	rosrun camera_calibration cameracalibrator.py --size 8x6 --square 0.108 right:=/ias/stereo/right/rotated/image left:=/ias/stereo/left/rotated/image right_camera:=/ias/stereo/right/rotated left_camera:=/ias/stereo/left/rotated --no-service-check --approximate=0.1
